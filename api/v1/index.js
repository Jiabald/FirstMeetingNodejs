const increaseRouter = require('./increase')
const delRouter = require('./del')
const changeRouter = require('./change')
const login = require('./login')
const query = require('./query')

module.exports = {
  increaseRouter,
  delRouter,
  changeRouter,
  login,
  query,
}
